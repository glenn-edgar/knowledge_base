CREATE TABLE knowledge_base.knowledge_base (
    id SERIAL PRIMARY KEY,
    label VARCHAR NOT NULL,
    name VARCHAR NOT NULL,
    properties JSON,
    data JSON,
    path LTREE,
    subgraph_id INTEGER NOT NULL,
    linked_subgraph_id INTEGER,
    UNIQUE (subgraph_id, path)
);
